version: "3.8"
services:
  asset_ai:
    image: nvcr.io/nvidia/nemo:latest
    deploy:
      replicas: 1
      resources:
        limits:
          memory: 8G
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - /mnt/nas/asset_data:/workspace/data
      - /mnt/nas/asset_models:/workspace/models
    environment:
      - SERVICE_TYPE=asset
      - DATA_DIR=/workspace/data
      - MODEL_PATH=/workspace/models/asset_model.pt
    networks:
      - dgxnet
    runtime: nvidia
    command: ["python", "/workspace/scripts/asset_inference.py", "--data_dir", "/workspace/data", "--model_path", "/workspace/models/asset_model.pt"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

networks:
  dgxnet:
    driver: bridge
